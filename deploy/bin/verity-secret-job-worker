#!/nodejs/bin/node

// Fixed absolute import: this launcher lives outside the package tree so the server modules and
// workspace dependencies continue resolving from /app exactly as built.
import('file:///app/packages/server/dist/secret-worker-bin.js').catch(() => {
  process.stderr.write('verity secret-job worker failed closed\n');
  process.exitCode = 1;
});
