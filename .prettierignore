**/dist/**
**/node_modules/**
coverage/**
package-lock.json
THIRD_PARTY_NOTICES.md
# Generated by release-please; its layout is owned by the release tool. The
# manifest is here for a second reason too: CI skips every job on a commit that
# touches only these files, so a formatter that read one would be a check the
# skip silently drops. Asserted in scripts/ci-workflow.test.ts.
CHANGELOG.md
**/CHANGELOG.md
.release-please-manifest.json
version.txt
# Reproducible esbuild output; format its typed sources instead.
features/verity-sandbox-toolkit/bin/verity-runner-worker.mjs
# Per-project dev-container scaffold — owned separately, not formatted here.
.dev-server/
# Local session metadata is not repository source.
.*-worktree.json
# Hand-authored design docs with deliberate layout (aligned code blocks, ASCII
# diagrams, tables); Prettier reflow would mangle them. Code/README stay formatted.
docs/
# Session inputs are never part of the exported repository.
verity-source-snapshot-*.tar.gz
