This mobile-only recovery flow does not alter a published Server artifact and
therefore must not create a backend release. A confirmed-not-started trusted CLI
materialization failure opens the existing unlock route and asks the same agent
to retry once after successful unlock.
