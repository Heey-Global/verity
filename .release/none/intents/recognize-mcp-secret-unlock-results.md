This mobile-only compatibility fix does not alter a published Server artifact and
therefore must not create a backend release. The existing unlock recovery now
recognizes the standard MCP call-result wrapper used by trusted CLI failures.
