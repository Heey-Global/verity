This mobile-only session recovery fix does not change a published Server artifact
and therefore must not create a backend release. Resume initial session loading
and stream authorization correctly after the app returns from the background.
