Do not cut a Server release for this native mobile build correction. Run native
dependency patches and shared workspace compilation inside the EAS archive before
prebuild. Trial a bounded, strictly validated compiler-object cache for TestFlight
builds, with cache statistics and separate build/upload/processing measurements.
The native mobile release train owns the resulting app change.
