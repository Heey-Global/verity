This mobile-only recovery change does not alter a published Server artifact and
therefore must not create a backend release. Let the app use the public secret
status as a safe fallback when the richer onboarding probe cannot route a sealed
store to its existing master-password unlock form.
