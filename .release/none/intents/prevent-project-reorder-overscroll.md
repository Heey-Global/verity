This mobile-only fix prevents project reordering from creating negative scroll
offsets or triggering pull-to-refresh during collapse and expansion. It does not
change a published Server artifact and must not create a backend release.
