This mobile-only recovery change does not alter a published Server artifact and
therefore must not create a backend release. Treat the public secret status as
authoritative so a confirmed sealed store reaches the existing unlock form even
when richer onboarding metadata is absent after an update.
