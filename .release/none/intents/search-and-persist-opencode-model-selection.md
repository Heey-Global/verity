Improve the mobile OpenCode provider settings with alphabetically sorted models
and search. Preserve queued model availability changes, restore confirmed choices
after failed saves, and prevent edits while credentials are locked. This changes
only the mobile app and requires no Server release or native runtime change.
