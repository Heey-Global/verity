Mask decoded file credentials as well as their encoded transport values in
trusted CLI stdout and stderr. Verify file injection and redaction through the
production supervisor client, a live worker, and the spawn broker.
