# Start with strict cache validation. React Native's bundled configuration enables
# sloppiness for modules, timestamps and headers; this pilot intentionally does not.
max_size = 512MiB
compiler_check = content
